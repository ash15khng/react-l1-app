import React from "react";

const Person = ({name, age}) => {
    return (
    <div>
        <h1>Hello World!</h1>
        <p>Name: {name}</p>
        <p>Age: {age}</p>
    </div>
    );
};

export default Person;